package geometries;

import primitives.Point;

public class Triangle extends Polygon {
    /**
     * Triangle ctor
     * @param vertices - params of type point
     */
    public Triangle(Point... vertices) {
        super(vertices);
    }
}
